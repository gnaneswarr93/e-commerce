class Product {
  final String id; // Ensure this field exists
  final String title; // Ensure this field exists
  final String description; // Ensure this field exists
  final double price; // Ensure this field exists
  final String imageUrl; // Ensure this field exists

  Product({
    required this.id,
    required this.title,
    required this.description,
    required this.price,
    required this.imageUrl,
  });
}
